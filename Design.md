# 100% トラッカー Design

100% トラッカー is a mobile-first local PWA for one personal user. The interface is built for repeated iPhone logging: dense, quiet, tap-first, and fast enough to use between meals or sets.

## Product Shape

- Five tabs: Home, Food, Workout, History, Settings.
- No account, no cloud, no user switching.
- IndexedDB owns all user data. LocalStorage only stores small UI preferences.
- Food logging favors search, favorites, personal menus, recommendations, chain search, recent-history reuse, and quick general estimates before manual entry.
- Chain seeds retain source-level provenance. For 生そばウエスト, the official calorie PDF (2025-02-03) covers the listed udon/soba, sets, bowls, MIX toppings, and soba additions; those entries use exact kcal with separately estimated PFC. Menu rows absent from that source remain explicitly estimated rather than inheriting an official-calorie label.
- ティーヌン presets are scoped to the イオンモール幕張新都心 food-court location rather than treated as a chain-wide menu. Published and recently observed store menu names are retained, non-published generic size controls are disabled, and nutrition stays explicitly estimated because the store menu does not publish kcal/PFC.
- Food AI photo logging is a bridge workflow: the app copies a prompt for the user's AI tool, accepts only JSON-like code output, normalizes and bounds the data, and first reports how many items were read. Each item then gets its own review screen. The AI-read name and nutrition are shown first, followed by existing-menu candidates with an always-visible "not in this list" action. Existing candidates are never auto-selected. When no candidate applies, a separate screen asks whether to log once, save and log, save only, or skip. Bridge v3 keeps the saved kcal/P/F/C as adopted values, retains the AI candidate separately, and supports per-nutrient source, confidence, exact/estimated state, uncertainty, quantity uncertainty, and optional parent/topping components. v1/v2 remain import-compatible. Unverified `possible_menu_name` values are matching hints, never default saved names.
- Workout logging favors templates, favorites, My Training, body-part drilldown, equipment drilldown, and stepper controls.
- My Training is the workout equivalent of My Menu: user-created exercises can be registered from an existing exercise or from scratch, saved with defaults and weight presets, edited in place, deleted without touching past logs, and reused from Workout or Settings.
- Settings always exposes JSON backup/import and reminds the user to back up local data.
- First launch starts on Welcome rather than treating restore as a mandatory setup step. A backup can be imported there, while new setup always collects the shared profile fields needed for calculation, including birth year. Before goal setup, activity can follow the original four-level quick choice, an optional detailed baseline wizard, or be deferred. Quick and detailed choices drive the initial automatic goal and save the same activity profile; deferring uses `moderate` only as a provisional goal input and schedules a later profile reminder without inventing average activity data.

## Visual Direction

- Mobile utility app, not marketing.
- Restrained off-white base, ink text, green status accents, clay warnings/actions.
- Theme Settings offers optional character backgrounds. The default remains no character; selecting one changes the app to a dark glass presentation and can be reversed without changing logs or the saved accent color. Characters are grouped as women, men, and other, then a style is selected so future costume unlocks can use the same flow. Current characters are SAGE, VOLT, NOVA, ARIA, TITAN, FLASH, GRIT (sports-photo style with a white singlet), AEGIS (comic masked hero), LUMEN, KORU, VITO, and KITSUNE.
- A selected character has low, mid, high, fail, cheat, and travel images. During normal daily progress, higher phases are unlocked on the Home hero card: tap "パワーを送る" 10 times for each pending phase. The unlocked stage is stored per character and app date; cheat, travel, and over-goal states keep their direct visual treatment.
- Character themes provide six state backgrounds: low, middle, high, goal miss, cheat day, and travel. The state image follows the same exception-day priority as Home, while all tabs share the background layer and keep the standard card and bottom-navigation placement. Assets are grouped by character and `default` variant so future trophy-unlocked clothing versions can be added without changing the character identity or state contract.
- Compact cards with 8px radius or less.
- Segmented tabs, chips, steppers, and icon buttons for common actions.
- Japanese-first labels because the preset database and daily flow are Japanese.

## Interaction Principles

- Every logged item is a snapshot.
- Adopted nutrition and interpretation are separate layers. The app never changes a saved kcal/P/F/C value because it is estimated; uncertainty only affects status language and the safe amount suggested for additional food. kcal/P/F/C provenance is field-level: an official calorie with estimated PFC is not a calorie estimate, and a product-label value with estimated quantity is represented as quantity uncertainty rather than relabeling the nutrition label as estimated.
- Estimation categories intentionally overlap. Reports distinguish calorie estimate, broad PFC estimate, strict PFC-only estimate, consumed-total estimate, internal composition estimate, and zero-kcal estimate. `PFC-only` requires an exact menu, exact consumed total, exact kcal, and estimated P/F/C; an item scaled from a photo-estimated bite is a quantity estimate instead. Legacy AI notes such as `AI推定量: 1口（約15g）` and `1杯（ご飯約220g、肉約80g想定）` are interpreted as consumed-total and internal-composition estimates respectively without changing their saved nutrition.
- User ownership and nutrition provenance are separate. An AI-created My Menu item remains user-owned but keeps `estimated` provenance and copies its nutrition metadata into every new food-log snapshot.
- Home stays compact: current-day deficits are treated as remaining room, while past days receive final interpretation. Estimate share and safe remaining values live behind a small detail control: kcal/F/C use conservative upper-bound room. Protein is never shown as an additional-food upper limit; it separately shows adopted P, the safety-side lower bound, and the possible gap to the goal, with clear copy that extra protein is not mandatory. Special-mode visuals take precedence.
- Protein safety copy has three distinct states shared by Home and AI report output: an adopted value below target shows the ordinary amount remaining (and only becomes a deficit after day completion); an adopted value at target with a lower uncertainty bound below target explains that extra intake is optional; and a lower bound at or above target reports that the target remains covered. Nutrition totals and uncertainty calculations are not changed by this presentation rule.
- The nutrition detail control is framed as a Home display preference, not a data-quality audit. Users choose either recorded remaining values or a slightly conservative view; the hero calorie number, progress, PFC grams, PFC percentages, and tap-to-show PFC remaining values all use the same basis. Saved nutrition never changes. Only the selected option is accented, while provenance and uncertainty details stay collapsed unless requested. Red over-target styling is reserved for adopted values that clearly exceed the target; a safety-only crossing uses the estimate tone.
- PFC status colors are directional: protein below 80% is a warning and protein at or above 80% is good; fat and carbs above 110% are warnings, 80-110% is good, and lower values are shown as low. Estimated fat overage within its uncertainty keeps the estimate tone.
- The Home check-in card can show either today's weight or the 7-day average. Body fat can show today's value, the 7-day average, or remain hidden. These are display preferences and never alter saved check-in records.
- Home goal access first shows a concise summary of the active calorie, PFC, phase, and target values. Editing continues into the focused Goal Settings flow instead of dropping the user at the Settings root.
- History places a body-part training summary between the body-fat chart and daily AI report. Chest, shoulders, arms, back, legs, and core always show the latest recorded date and elapsed days. A donut chart uses recorded set count, with a one-record fallback for legacy exercises without sets, and can switch between all-time, current month, current week, and the current app date. Cardio is excluded from this strength balance.
- User-facing copy explains the outcome or next action. Internal storage names, implementation details, and visual-system notes do not appear in ordinary guidance text.
- Recommendation surfaces use adopted remaining values alongside safety-adjusted planning values. AI reports calculate estimate share only from entries whose calories are estimated, show separate counts for calorie/PFC/quantity/0kcal uncertainty, and never treat the safety buffer as consumed nutrition.
- Chain combination recommendations are availability-aware. The modal defaults to the current meal period and dine-in service unless a selected seed item is explicitly breakfast-only or takeout-only. Users can switch between dine-in/takeout and morning/lunch/dinner; menu names, categories, and tags marked as morning, lunch, dinner, takeout, or eat-in are filtered before combinations are scored, while unmarked items remain available rather than being guessed unavailable.
- Chain discovery can switch between cuisine genre and Japanese kana rows without losing the selected brand. Combination scoring first rejects duplicate mains and implausible carb pairings, then adds cuisine-aware weights for complete sets, familiar sides, toppings, and trusted size variants before applying the nutrition-fit score.
- The current day is always in progress: all user-facing goal differences say "remaining" or "to go" rather than "deficient". Final under/over language is reserved for past dates. Mixed workout sessions are derived from their exercises and count toward both strength and cardio when applicable.
- Missing food days are displayed as missing, never counted as zero.
- Goal changes update targets going forward without mutating old entries.
- Manual entry exists, but quick logging is the default path.
- Exception days such as cheat day, travel modes, and pause mode keep streaks intact and visually mark Home goal evaluation as special handling. Their hidden goal delta must not leak normal-day over-target warning colors into the hero value or PFC pills. During travel mode, the destination badge is an explicit visibility control for the hero calorie and PFC values; revealing them does not restore normal-day warning colors or replace the full rainbow status bar. While revealed, an adjacent settings control opens the same recorded-versus-conservative remaining selector used on normal days.
- Settings is hierarchical like iOS Settings. Current top-level groups are AI report, Activity profile, Export, Goal, Theme, Record settings, My Menu, My Training, and General.
- Activity data has two separate layers. `Settings.activity_profile` stores the user's normal baseline, initialized from the goal activity level and optionally enriched with 30-day/4-week average steps, active calories, exercise minutes, and notes. Each AI report separately captures a qualitative target-day comparison plus optional manually entered values and source. Target-day activity never modifies kcal/PFC targets or food totals; profile changes affect automatic goals only after the user reviews and applies the recalculation. A current-day report defaults to partial coverage; a past-day report defaults to completed coverage, and both remain user-selectable where the report is created.
- The activity profile's qualitative activity level is the source for automatic goal calculation. Average steps, Move calories, and exercise minutes support that classification but are never added directly to TDEE. Profile changes show the current and recalculated targets before application; custom kcal/PFC remain untouched and receive a reference calculation only. Changing activity level inside Goal Settings synchronizes the same profile field.
- Target-day activity uses either a one-tap qualitative comparison or manual details. Opening manual entry clears the qualitative comparison and shows only optional numeric fields, source, and notes so the two input modes cannot appear selected at the same time.
- The activity profile may store an optional source for its average values. Existing profiles remain valid and report `保存元不明`; no IndexedDB schema migration is required because the profile is an optional object inside Settings. Report output evaluates profile source/confidence separately from target-day source/confidence, and a qualitative-only target day explicitly says that numerical evaluation is unavailable.
- AI report activity guidance treats low/normal/high as qualitative context, never converts Move calories directly into food allowance, never infers total daily activity from workout sessions alone, and explicitly reports missing activity data rather than filling it with guesses.
- AI report creation is a six-screen wizard: period (today/7 days/30 days), new or existing AI chat, food/workout/both, target-day activity, optional question, then generation and output. A daily report retains partial/completed selection on the period screen. Copy and Markdown-save actions stay above the preview.
- Daily AI reports label nutrition as `当日集計` with day totals, while multi-day reports retain `食事平均`. A new-chat report keeps the complete instructions and baseline. An existing-chat report always repeats the target period, generated time, core profile/goal values, and cheat/travel/pause state, then sends only the selected food/workout data with a short continuing-rule block. Multi-day existing-chat reports omit individual food rows and workout set detail in favor of aggregates; daily reports retain the selected detail. Generating does not advance the conversation baseline; a successful copy does. Each saved report retains its complete source Markdown, delivery mode, content scope, copied time, conversation anchor, and previous report link as optional fields, preserving IndexedDB and backup compatibility.
- AI report generation audits food-record coverage before producing the report. Past/completed days without food entries are reviewed one day at a time; current partial days and pause-mode days are not treated as missing. Qualitative answers are stored in the optional `Settings.food_record_contexts` map and never converted into calories automatically. Rough kcal/PFC values remain report-only reference data and do not alter adopted food totals, goals, achievements, or streaks. Reports explicitly separate recorded-day averages from full-period averages and never interpret an unresolved or cheat-day gap as 0 kcal.
- Registered My Menu and My Training entries support overwrite editing so small variations do not create accidental duplicates.
- Trophies reward first-use moments and long-term consistency. New feature adoption includes AI Photo logging and My Training registration; streak milestones currently include 3, 7, 14, 30, 45, 60, 75, 100, 150, and 365 days. The trophy sheet doubles as a guide: it highlights near-term targets, shows progress and remaining actions, and update notes can route directly into the trophy guide after trophy-related releases.
- Food size variants are introduced as a reversible presentation layer first. Chain menu rows that clearly represent the same item with size-only differences can be grouped in search results and expanded into size choices at logging time, while the original menu records and past log snapshots remain untouched. Parent rows and active add sheets hide size-only suffixes such as `(並盛り)`, but the selected size is still preserved in the logged menu snapshot. Do not force this onto sushi, fixed-count items, or products where the serving unit is the menu identity.
- サーティワンのフレーバーは、公式のキッズ／スモール・レギュラー・ポップ別kcal/PFCをサイズ候補として保持する。記録時はサイズの後にカップ・シュガーコーン・ワッフルコーンを選び、複数スクープでも容器は1個分だけ加算する。コーンは公式公開kcalを固定し、非公開のPFCだけを推定として扱う。
- すき家は公式栄養成分PDFの全表を単一の正本として取り込み、牛丼だけでなく期間商品、カレー、各種丼、定食、朝食、お子様、セット、サイド、ドリンク、デザートまで現行行を保持する。PDF更新時は全行を再取得し、旧補完seedのすき家行を併用しない。
- Set meals adjust realistic staple components instead of scaling the full item. Rice-only sets expose rice amount, noodle-only sets expose noodle amount, and mixed bowl-plus-noodle sets expose noodle and rice/bowl amounts separately while keeping toppings, sides, and soup fixed.
- Published pasta sizes are calculated relative to the nutrition snapshot's own serving size. An AI-adopted Mamma Pasta large serving at cooked 375g therefore maps to the large option at 1.0x; selecting large must never apply another 1.5x increase to an already-large snapshot.
